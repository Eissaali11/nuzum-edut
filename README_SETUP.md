# دليل تثبيت وتشغيل مشروع نُظم
# Nuzum Project - Setup Guide

## 📋 المتطلبات الأساسية | Requirements

### قبل البدء، تأكد من تثبيت:

- ✅ **Node.js** - مثبت (v24.12.0)
- ✅ **npm** - مثبت (11.6.2)
- ⚠️ **Python 3.8+** - يجب تثبيته

---

## 🐍 خطوة 1: تثبيت Python

### الطريقة الأولى: من الموقع الرسمي

1. اذهب إلى: https://www.python.org/downloads/
2. حمّل أحدث نسخة Python 3.x
3. **مهم جداً:** أثناء التثبيت، ضع علامة ✓ على **"Add Python to PATH"**
4. أكمل التثبيت

### الطريقة الثانية: باستخدام winget (Windows)

افتح PowerShell كمسؤول وشغّل:

```powershell
winget install Python.Python.3.12
```

### التحقق من التثبيت

بعد التثبيت، افتح PowerShell جديد وشغّل:

```powershell
python --version
```

يجب أن تظهر رسالة مثل: `Python 3.12.x`

---

## 🚀 خطوة 2: تشغيل المشروع

### الطريقة السريعة (موصى بها)

افتح PowerShell في مجلد المشروع وشغّل:

```powershell
.\setup_and_run.ps1
```

هذا السكريبت سيقوم بكل شيء تلقائياً:
- إنشاء البيئة الافتراضية
- تثبيت جميع المكتبات
- إعداد قاعدة البيانات
- تشغيل المشروع

### الطريقة اليدوية

إذا فضلت تشغيل الأوامر يدوياً:

```powershell
# 1. إنشاء البيئة الافتراضية
python -m venv venv

# 2. تفعيل البيئة الافتراضية
.\venv\Scripts\Activate.ps1

# 3. ترقية pip
python -m pip install --upgrade pip

# 4. تثبيت المكتبات (قد يستغرق عدة دقائق)
python -m pip install -r requirements.txt

# 5. نسخ ملف البيئة
Copy-Item .env.local .env

# 6. إعداد قاعدة البيانات
$env:FLASK_APP = "app.py"
python -m flask db upgrade

# 7. تشغيل المشروع
python app.py
```

---

## 🌐 الوصول إلى المشروع

بعد تشغيل المشروع، افتح المتصفح على:

**http://127.0.0.1:5000**

أو

**http://localhost:5000**

---

## 🛠️ معلومات المشروع

### نوع المشروع
- **Framework:** Flask (Python)
- **Database:** SQLite (للتطوير المحلي)
- **Frontend:** HTML/CSS/JavaScript

### المكتبات المثبتة
- ✅ **Python packages:** 81 مكتبة من `requirements.txt`
- ✅ **Node.js packages:** 28 حزمة من `package.json`
- ✅ **أمان npm:** تم إصلاح الثغرات الأمنية

### الملفات المهمة
- `app.py` - ملف التطبيق الرئيسي
- `.env.local` - إعدادات التطوير المحلي
- `requirements.txt` - مكتبات Python
- `package.json` - مكتبات Node.js

---

## ❓ حل المشاكل الشائعة

### مشكلة: PowerShell لا يسمح بتشغيل السكريبت

إذا ظهرت رسالة خطأ عند تشغيل السكريبت، شغّل هذا الأمر كمسؤول:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### مشكلة: Python غير موجود بعد التثبيت

1. أعد تشغيل PowerShell
2. تأكد من أنك اخترت "Add Python to PATH" أثناء التثبيت
3. إذا استمرت المشكلة، أعد تثبيت Python

### مشكلة: فشل تثبيت بعض المكتبات

بعض المكتبات مثل `cairocffi` قد تحتاج إلى مكتبات نظام إضافية.
السكريبت سيستمر حتى لو فشل تثبيت بعض المكتبات.

---

## 📞 الدعم

في حالة وجود أي مشاكل، تأكد من:
1. تثبيت Python بشكل صحيح
2. تفعيل البيئة الافتراضية
3. وجود اتصال بالإنترنت لتثبيت المكتبات

---

## ✅ قائمة التحقق

- [ ] تم تثبيت Python 3.8+
- [ ] تم إضافة Python إلى PATH
- [ ] تم تشغيل `setup_and_run.ps1`
- [ ] المشروع يعمل على http://localhost:5000

---

تم الإعداد بنجاح! 🎉
