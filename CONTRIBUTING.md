# 🤝 دليل المساهمة في NUZUM
## Contributing to NUZUM Project

شكراً على اهتمامك بالمساهمة في مشروع NUZUM! نحن نرحب بجميع المساهمات.

---

## 📋 قواعد السلوك

كل المساهمين في هذا المشروع ملتزمون بـ Code of Conduct الخاص بنا:

- ✅ احترم آراء الآخرين
- ✅ تحدث بأدب واحترافية
- ✅ ركز على الكود، لا على الشخص
- ✅ ساعد الآخرين في التعلم
- ✅ لا تتحدث بأسلوب فظ أو مسيء

---

## 🚀 طرق المساهمة

### 1. الإبلاغ عن الأخطاء (Bug Reports)

وجدت خطأ؟ أخبرنا به!

**اذهب إلى:** [GitHub Issues](../../issues)

**ملء جميع التفاصيل:**
```
العنوان: خطأ واضح وموجز

الوصف:
- ما الذي حاولت؟
- ما الخطأ الذي حدث؟
- ما الذي توقعت أن يحدث؟

خطوات التكرار:
1. ...
2. ...
3. ...

معلومات البيئة:
- نظام التشغيل: Windows 10
- إصدار Python: 3.9
- إصدار Flask: 2.3.0
```

### 2. اقتراح ميزات جديدة (Feature Requests)

لديك فكرة رائعة؟ اقترحها!

**اذهب إلى:** [GitHub Issues - Feature Request](../../issues)

**الصيغة:**
```
العنوان: ميزة: وصف الميزة

الوصف:
- المشكلة التي تحلها هذه الميزة
- الحل المقترح
- حالات الاستخدام الممكنة

أمثلة بديلة:
- الحلول الأخرى التي فكرت فيها

و معايير القبول:
- [ ] يعمل على الويب
- [ ] يعمل على الموبايل
- [ ] يشمل التوثيق
```

### 3. تقديم Pull Request (Code Contribution)

هذا الخيار الأفضل للمساهمة المباشرة!

#### الخطوة 1: Fork المشروع
```bash
# على GitHub، اضغط زر "Fork"
```

#### الخطوة 2: استنسخ Fork المحلي
```bash
git clone https://github.com/YOUR_USERNAME/NUZUM.git
cd NUZUM
```

#### الخطوة 3: أنشئ فرع جديد
```bash
# للميزات الجديدة
git checkout -b feature/amazing-feature

# لإصلاح الأخطاء
git checkout -b fix/bug-name

# لتحسين الأداء
git checkout -b perf/optimization-name
```

#### الخطوة 4: عمل التغييرات

**اتبع معايير الكود:**
```python
# ✅ صحيح - أسماء واضحة
def calculate_attendance_percentage(employee_id, month):
    """
    حساب نسبة الحضور للموظف.
    
    Args:
        employee_id (int): معرّف الموظف
        month (int): الشهر (1-12)
    
    Returns:
        float: نسبة الحضور
    """
    pass

# ❌ خاطئ - أسماء غير واضحة
def calc(e, m):
    """حساب"""
    pass
```

#### الخطوة 5: Commit التغييرات
```bash
git add .
git commit -m "feat: add attendance calculator"
git commit -m "fix: logout redirect to login"
git commit -m "docs: update README"
git commit -m "refactor: move helpers to utils"
```

**صيغة Commit Message:**
```
<نوع>: <الوصف>

<الجسم - اختياري>

<التذييل - اختياري>
```

**الأنواع:**
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تحديث التوثيق
- `style`: تنسيق الكود (بدون تغيير منطقي)
- `refactor`: إعادة صياغة (بدون تغيير الوظيفة)
- `perf`: تحسين الأداء
- `test`: إضافة اختبارات

#### الخطوة 6: Push للفرع
```bash
git push origin feature/amazing-feature
```

#### الخطوة 7: اطلب دمج (Pull Request)

1. اذهب إلى GitHub
2. ستجد زر "Compare & Pull Request"
3. اضغط عليه
4. **ملء التفاصيل:**

```markdown
## الوصف
وصف موجز لما يفعله PR

## النوع
- [ ] إضافة ميزة جديدة
- [ ] إصلاح خطأ
- [ ] تكسير تغيير (Breaking change)

## كيفية الاختبار
الخطوات للتحقق من التغييرات:
1. ...
2. ...

## قائمة التحقق
- [ ] اتبعت معايير الكود
- [ ] أضفت التوثيق
- [ ] اختبرت التغييرات محلياً
- [ ] لم أضع بيانات حساسة
- [ ] رسالة commit واضحة
```

---

## 📐 معايير الكود

### Python

```python
# أسلوب التسمية
class AttendanceService:  # PascalCase للفئات
    MAX_ATTEMPTS = 3      # UPPER_CASE للثوابت
    
    def calculate_stats(self):  # snake_case للدوال
        employee_id = 1   # snake_case للمتغيرات
        pass

# التنسيق
# - استخدم 4 مسافات للمحاذاة
# - سطر فارغ بين الدوال
# - docstrings لكل دالة

# الطول الأقصى للسطر: 120 حرف
```

### HTML/CSS

```html
<!-- استخدم معايير W3C -->
<div class="attendance-list">
  <!-- استخدم BEM للفئات -->
  <div class="attendance-list__item">
    <span class="attendance-list__item--active">حاضر</span>
  </div>
</div>
```

```css
/* استخدم SCSS إذا أمكن */
.attendance-list {
  display: grid;
  
  &__item {
    padding: 1rem;
  }
  
  &__item--active {
    color: green;
  }
}
```

---

## 🧪 الاختبارات

### كتابة الاختبارات

```python
import unittest

class TestAttendance(unittest.TestCase):
    def setUp(self):
        """إعداد الاختبار"""
        self.service = AttendanceService()
    
    def test_calculate_percentage(self):
        """اختبار حساب النسبة"""
        result = self.service.calculate_percentage(1, 1)
        self.assertEqual(result, 95.5)
    
    def test_invalid_employee(self):
        """اختبار موظف غير موجود"""
        with self.assertRaises(ValueError):
            self.service.calculate_percentage(-1, 1)
```

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
python -m pytest

# تشغيل ملف واحد
python -m pytest tests/test_attendance.py

# تشغيل دالة واحدة
python -m pytest tests/test_attendance.py::TestAttendance::test_calculate_percentage
```

---

## 📚 التوثيق

### Docstrings

```python
def calculate_stats(employee_id: int, month: int) -> dict:
    """
    حساب إحصائيات الحضور للموظف.
    
    حساب مفصل لنسبة الحضور والغياب والرصيد 
    المتبقي للموظف في شهر معين.
    
    Args:
        employee_id (int): معرّف الموظف الفريد
        month (int): رقم الشهر (1-12)
    
    Returns:
        dict: قاموس يحتوي على:
            - 'presence_percentage' (float): نسبة الحضور
            - 'absence_count' (int): عدد الغيابات
            - 'overtime_hours' (float): ساعات العمل الإضافي
    
    Raises:
        ValueError: إذا كان employee_id سالب
        ValueError: إذا كان month خارج النطاق 1-12
    
    Example:
        >>> stats = calculate_stats(1, 2)
        >>> print(stats['presence_percentage'])
        95.5
    """
    pass
```

### تحديث README

إذا أضفت ميزة جديدة:
1. أضفها إلى القسم المناسب في [README.md](README.md)
2. أضف مثال استخدام
3. أضفها إلى جدول المحتويات

---

## 🔄 عملية المراجعة

### ماذا يتوقع من المراجع:

1. **الاختبار**: اختبار التغييرات محلياً
2. **المراجعة**: مراجعة الكود سطراً بسطر
3. **التعليقات**: إضافة تعليقات بناءة
4. **الموافقة**: الموافقة أو طلب تعديلات

### استجابتك:

- رد على التعليقات بسرعة
- اقبل النقد البناء
- اطلب توضيحات إن لم تفهم
- كن لطيفاً وإيجابياً

---

## 📦 عملية الإطلاق

### للمشرفين فقط:

```bash
# زيادة الإصدار
# X.Y.Z -> X.(Y+1).0 للإطلاق العام
# X.Y.Z -> X.Y.(Z+1) للإصلاحات

# مثال: من 1.0.0 إلى 1.1.0
git tag -a v1.1.0 -m "Release v1.1.0"
git push origin v1.1.0
```

---

## ❓ أسئلة معروفة

### س: كيف أبدأ؟
**ج:** اقرأ [QUICK_START.md](QUICK_START.md) و [SOLUTION.md](SOLUTION.md)

### س: هل يمكنني إصلاح الأخطاء الإملائية؟
**ج:** نعم! حتى الأخطاء الصغيرة مهمة.

### س: ماذا إذا كانت فكرتي مشابهة لـ PR آخر؟
**ج:** ليس هناك مشكلة، سيتم دمج الأفضل منهما.

### س: كم من الوقت تأخذ المراجعة؟
**ج:** عادة 1-3 أيام حسب التعقيد.

### س: هل سأحصل على رصيد؟
**ج:** نعم! كل PR سيُدرج في [CONTRIBUTORS.md](CONTRIBUTORS.md)

---

## 🎓 موارد تعليمية

- **تعلم Git:** https://git-scm.com/book
- **GitHub Flow:** https://guides.github.com/introduction/flow/
- **معايير Python:** https://peps.python.org/pep-0008/
- **البرامج التعليمية:** https://www.freecodecamp.org/

---

## 📧 التواصل

- **Issues:** للتفاصيل التقنية
- **Discussions:** للأفكار والنقاش العام
- **Email:** admin@nuzum.local

---

<div align="center">

### شكراً لمساهمتك! 🎉

كل Pull Request يجعل NUZUM أفضل.

</div>
