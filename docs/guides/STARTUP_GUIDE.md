# نُظم - Startup & Configuration Guide
## دليل التشغيل والتكوين

---

## 📚 ملفات الحل الجذري

### الملفات الجديدة المضافة:

| الملف | الوصف | الاستخدام |
|-------|-------|-----------|
| **startup.py** | Python startup script موحد | `python startup.py` |
| **start.bat** | Windows batch script | Double-click أو `start.bat` |
| **start.ps1** | PowerShell script متقدم | `.\start.ps1` |
| **.env.example** | ملف التكوين المركزي | المرجع للإعدادات |
| **health_check.py** | فحص صحة النظام | `python health_check.py` |
| **SOLUTION.md** | شرح الحل الجذري | القراءة قبل الاستخدام |

---

## 🚀 طرق التشغيل

### الطريقة الأولى: Python (الموصى بها)
```bash
cd D:\nuzm
python startup.py
```
**المميزات:**
- ✓ يعمل على أي نظام
- ✓ مع فحوصات تلقائية
- ✓ رسائل خطأ واضحة

### الطريقة الثانية: Windows Batch
```
1. افتح File Explorer
2. اذهب إلى D:\nuzm
3. اضغط على start.bat
```
**المميزات:**
- ✓ بسيط جداً
- ✓ لا يتطلب أوامر
- ✓ يعمل مباشرة

### الطريقة الثالثة: PowerShell
```powershell
cd D:\nuzm
.\start.ps1
```
**المميزات:**
- ✓ متقدم وقابل للتخصيص
- ✓ معالجة أخطاء أفضل
- ✓ خيارات إضافية

---

## ✅ قائمة التحقق قبل التشغيل

### تشغيل فحص صحة النظام:
```bash
python health_check.py
```

يجب أن ترى:
```
✓ app.py موجود
✓ startup.py موجود
✓ Database موجود
✓ Static folder موجود
✓ Templates folder موجود
✓ CSS files موجودة
✓ البوابة مضبوطة على 5000

🎉 النظام سليم تماماً - جاهز للعمل!
```

---

## 🔧 تعديل الإعدادات

### إذا أردت تغيير الإعدادات:

1. **انسخ .env.example إلى .env**
   ```bash
   copy .env.example .env
   ```

2. **عدّل .env بأي محرر نصي**
   ```
   PORT=5000
   FLASK_DEBUG=False
   ATTENDANCE_USE_MODULAR=0
   ```

3. **شغل startup.py**
   ```bash
   python startup.py
   ```

---

## 📊 الإعدادات الموصى بها

### للإنتاج (الافتراضي)
```ini
PORT=5000
FLASK_DEBUG=False
ATTENDANCE_USE_MODULAR=0
FLASK_ENV=production
```

### للتطوير (غير موصى به)
```ini
PORT=5000
FLASK_DEBUG=True           # ⚠️ أبطأ قليلاً
ATTENDANCE_USE_MODULAR=0   # ✓ احتفظ بـ 0
FLASK_ENV=development
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "PORT already in use"
```bash
# حل: أغلق العملية القديمة
netstat -ano | findstr :5000
taskkill /PID <PID> /F
```

### المشكلة: "Module not found"
```bash
# حل: أعد تثبيت المتطلبات
pip install -r requirements.txt
```

### المشكلة: "Database not found"
```bash
# حل: تأكد من:
# D:\nuzm\instance\nuzum_local.db موجود
# إذا لم يكن، انسخه من النسخة الاحتياطية
```

---

## 📈 المراقبة المستمرة

### تشغيل فحص صحة النظام دورياً:
```bash
# يومياً قبل بدء العمل
python health_check.py

# أسبوعياً للصيانة
python health_check.py
```

---

## 🎯 الأهداف المحققة

✓ **استقرار تام**
- البوابة صحيحة دائماً (5000)
- الملفات الثابتة تحمّل بنجاح

✓ **سهولة الاستخدام**
- تشغيل واحد فقط
- لا مزيد من الأوامر المعقدة

✓ **توثيق كامل**
- كل إعداد موثق
- قيم افتراضية آمنة

✓ **قابلية الصيانة**
- سهل التعديل
- سهل التصحيح

---

## 🔄 العملية الموصى بها

### في البداية:
```bash
1. python health_check.py      # تحقق من جاهزية النظام
2. python startup.py           # ابدأ الخادم
```

### للاستخدام المنتظم:
```
1. Double-click على start.bat
   أو
   python startup.py
```

### للصيانة:
```bash
1. python health_check.py      # فحص دوري
2. تعديل .env إذا لزم
3. python startup.py           # إعادة تشغيل
```

---

## 📞 المساعدة السريعة

| المشكلة | الحل |
|--------|------|
| لا أعرف كيف أشغل الخادم | استخدم `start.bat` |
| أريد تغيير البوابة | عدّل البوابة في startup.py (تجنب هذا!) |
| الخادم بطيء | تأكد من `FLASK_DEBUG=False` |
| هناك أخطاء في الملفات | شغل `python health_check.py` |
| أريد قيم افتراضية مختلفة | انسخ `.env.example` إلى `.env` وعدّل |

---

## 🎉 الخلاصة

بهذا الحل:
- ✅ لن تواجه مشاكل البوابة مرة أخرى
- ✅ جميع الملفات ستحمّل بنجاح
- ✅ التشغيل سيكون سهلاً وموثوقاً
- ✅ الصيانة أصبحت أسهل

**كل شيء معد ومختبر ✓**

---

**آخر تحديث:** 22 فبراير 2026  
**الإصدار:** 1.0  
**الحالة:** ✅ Production Ready
