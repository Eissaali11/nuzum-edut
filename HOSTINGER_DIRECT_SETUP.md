# إعداد الربط المباشر مع Hostinger - دليل سريع

## 🎯 اختر طريقة الربط

### ✅ الطريقة 1: Git + Auto-Deploy (الأفضل)

**المميزات:**
- ✅ تحديث تلقائي عند الـ push
- ✅ سريع وآمن
- ✅ نسخ احتياطي تلقائي

**الخطوات:**

1. **أنشئ Git Repository**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/nuzum.git
   git push -u origin main
   ```

2. **في Hostinger hPanel**:
   - `Advanced` → `Git` → `Create Repository`
   - أدخل رابط Repository
   - فعّل `Auto Deploy`
   - اختر `Directory`: `public_html/nuzum`

3. **الآن**: كلما فعلت `git push`، سيتم التحديث تلقائياً!

---

### ✅ الطريقة 2: FTP مباشر (FileZilla)

**الخطوات:**

1. **احصل على بيانات FTP من Hostinger**:
   - `Files` → `FTP Accounts`
   - احفظ: Host, Username, Password

2. **استخدم السكريبت التلقائي**:
   ```powershell
   # أنشئ ملف .hostinger_config.json
   # ثم شغّل:
   .\deploy_to_hostinger.ps1
   ```

3. **أو استخدم FileZilla يدوياً**:
   - حمّل FileZilla
   - اتصل بـ Hostinger
   - اسحب الملفات من المحلي إلى الخادم

---

### ✅ الطريقة 3: PowerShell Script (تلقائي)

**الخطوات:**

1. **أنشئ ملف `.hostinger_config.json`**:
   ```json
   {
       "FtpHost": "ftp.yourdomain.com",
       "FtpUser": "your_username",
       "FtpPass": "your_password",
       "RemotePath": "/public_html/nuzum/"
   }
   ```

2. **شغّل السكريبت**:
   ```powershell
   .\deploy_to_hostinger.ps1
   ```

3. **سيتم رفع جميع الملفات تلقائياً!**

---

## 📋 قائمة التحقق

### قبل الربط:
- [ ] لديك حساب Hostinger نشط
- [ ] أنشأت Python App في hPanel
- [ ] أنشأت قاعدة بيانات MySQL
- [ ] حصلت على بيانات FTP

### أثناء الربط:
- [ ] رفعت الملفات (استثناء venv و __pycache__)
- [ ] أعددت ملف `.env` بقاعدة البيانات
- [ ] ثبتت المكتبات في Python App
- [ ] أعددت Start Command

### بعد الربط:
- [ ] اختبرت الموقع
- [ ] تحققت من قاعدة البيانات
- [ ] فعّلت SSL
- [ ] أنشأت البيانات التجريبية

---

## 🚀 البدء السريع

### للربط التلقائي (Git):
```bash
git push origin main
# ✅ تم! سيتم التحديث تلقائياً
```

### للربط اليدوي (FTP):
```powershell
.\deploy_to_hostinger.ps1
# ✅ تم! تم رفع جميع الملفات
```

---

## 📞 ملفات مساعدة

- `HOSTINGER_DIRECT_CONNECTION.md` - دليل مفصل
- `deploy_to_hostinger.ps1` - سكريبت رفع تلقائي
- `.hostinger_config.example.json` - مثال للإعدادات

---

**اختر الطريقة المناسبة لك وابدأ! 🎉**

